<!-- app/views/superadmin/taxbranches/show.html.erb -->
<!-- Tabs azioni -->
<div class="border-b border-slate-200 mb-4">
  <nav class="-mb-px flex space-x-6" aria-label="Tabs">
    <% tabs = [
      ["📄 Show", superadmin_taxbranch_path(@taxbranch)],
      ["🌐 Mappa di posizionamento", positioning_superadmin_taxbranch_path(@taxbranch)],
      ["📝 Post", new_post_path(taxbranch_id: @taxbranch.id)],
      ["＋ Aggiungi concetto", superadmin_taxbranch_tag_positionings_path(@taxbranch)]
    ] %>

    <% tabs.each do |label, path| %>
      <%= link_to label, path,
        class: class_names(
          "whitespace-nowrap border-b-2 px-3 py-2 text-sm font-medium",
          current_page?(path) ?
            "border-blue-500 text-blue-600" :
            "border-transparent text-slate-500 hover:border-slate-300 hover:text-slate-700"
        )
      %>
    <% end %>
  </nav>
</div>

User: <%= Current.user&.id %>
Lead: <%= Current.user&.lead&.id %>
<!-- Breadcrumb + Title + Actions -->
<div class="mb-4 flex items-start justify-between gap-3">
  <div class="flex-1 min-w-0">
    <nav class="text-sm text-slate-600">
      <%= taxbranch_breadcrumb(@taxbranch, admin: true) %>
    </nav>
    
    
  </div>

  
</div>
<div class="mt-6">
  <%= link_to "⬅️ Torna su",
              (@taxbranch.parent ? superadmin_taxbranch_path(@taxbranch.parent) : superadmin_taxbranches_path),
              class: "text-gray-600 hover:underline" %>
</div>
<%= render  "node", node: @taxbranch %>
<hr>





<div class="container  mt-6 mx-auto px-4 sm:px-6 lg:px-8">
  <h2 class="text-2xl font-bold mb-4">Post associato</h2>

<%# Render in base alla modalità %>
<a id="post-form"></a>
<%= render "posts/form", post: @post, taxbranch: @taxbranch %>
</div>